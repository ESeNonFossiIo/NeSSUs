.Phony: clean

clean:
	@echo "======================================================================"
	@echo " ---> removing *~ *.e* *.o* _tmp_* *.bak* files"
	@rm -rf *~ *.e* *.o* *.bak* _job_* _tmp_*
	@echo "======================================================================"